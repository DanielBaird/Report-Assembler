// Generated by CoffeeScript 1.3.3
(function() {

  window.RA || (window.RA = {});

  RA.Views || (RA.Views = {});

  RA.Views.DatasetList = Backbone.View.extend({
    tagName: 'div',
    className: 'datasets',
    initialize: function() {
      return console.log("init-ing a RA.Views.DatasetList");
    },
    render: function() {
      var html;
      html = "<h2>Datasets</h2>";
      this.$el.html(html);
      _.each(this.model.models, function(set) {
        return this.$el.append(new RA.Views.SingleDataset({
          model: set
        }).render().el);
      }, this);
      console.log('rendered DatasetList');
      return this;
    }
  });

  RA.Views.SingleDataset = Backbone.View.extend({
    tagName: 'div',
    className: 'singleDataset',
    initialize: function() {
      return console.log("init-ing a RA.Views.SingleDataset");
    },
    render: function() {
      var html, name;
      html = '';
      name = this.model.get('name');
      html += "<h3 class='name'>" + name + "</h3>";
      _.each(this.model.get('vars'), function(varvalue, varname) {
        return html += "" + varname + ": " + varvalue + "<br>";
      });
      this.$el.append(html);
      return this;
    }
  });

}).call(this);
